# 部署报告

## 报告信息
- **功能名称**：sales-crm
- **版本**：1.1
- **部署日期**：2026-04-15
- **部署者**：DevOps Agent

## 摘要

> Boss Agent 请优先阅读本节获取部署结果。

- **部署状态**：✅ 成功
- **访问地址**：本地验证地址 `http://127.0.0.1:8091`
- **部署环境**：本地开发验证
- **服务健康**：正常
- **回滚命令**：删除 `database/crm.sqlite` 后执行 `php scripts/reset-db.php`
- **增量交付**：已包含 KPI 看板与 `manager / sales` 基础角色权限

---

## 1. 部署概要

### 1.1 部署信息

| 项目 | 值 |
|------|-----|
| **项目类型** | 零依赖 PHP 单体 Web 应用 |
| **运行时** | PHP |
| **运行时版本** | 8.5.4 |
| **部署方式** | 本地开发 |

### 1.2 部署状态

| 阶段 | 状态 | 说明 |
|------|------|------|
| 依赖安装 | 🟢 成功 | 无 Composer / npm 安装步骤 |
| 构建 | ⚪ 跳过 | 原生 PHP 项目无需构建 |
| 启动服务 | 🟢 成功 | `php scripts/serve.php 127.0.0.1:8091` 正常启动 |
| 健康检查 | 🟢 成功 | `/health` 返回 200 |

## 2. 访问信息

### 🎉 应用已成功部署！

| 访问类型 | 地址 |
|----------|------|
| **本地访问** | http://127.0.0.1:8091 |
| **健康检查** | http://127.0.0.1:8091/health |

### 快速验证

```bash
php scripts/serve.php 127.0.0.1:8091
curl http://127.0.0.1:8091/health
open http://127.0.0.1:8091/login
```

## 3. 部署步骤详情

### 3.1 环境检查

```bash
php -v
```

**结果**：
- 项目类型：零依赖 PHP 单体
- 包管理器：无
- 入口文件：`public/index.php`

### 3.2 依赖安装

```bash
# 无需额外安装依赖
```

**输出摘要**：

```text
项目直接基于系统 PHP 8.5 运行，无 Composer / npm 安装步骤。
```

**状态**：🟢 成功

### 3.3 构建（如需要）

```bash
# 无构建步骤
```

**输出摘要**：

```text
原生 PHP 项目，无需构建产物。
```

**构建产物**：
- 目录：无
- 大小：无

**状态**：⚪ 跳过

### 3.4 启动服务

```bash
php scripts/serve.php 127.0.0.1:8091
```

**服务信息**：

| 项目 | 值 |
|------|-----|
| PID | 临时验证会话，未长期保留 |
| 端口 | 8091 |
| 启动时间 | 2026-04-15 16:33:44 +0800 |

**状态**：🟢 成功

## 4. 健康检查

### 4.1 检查结果

| 检查项 | 状态 | 响应时间 | 说明 |
|--------|------|----------|------|
| 服务启动 | 🟢 正常 | - | 本地开发服务器成功启动 |
| 端口监听 | 🟢 正常 | - | 8091 端口可访问 |
| 登录页响应 | 🟢 正常 | 20.24ms | HTTP 200，演示账号列表可见 |
| API 响应 | 🟢 正常 | 21.96ms | `/health` 返回 JSON |

### 4.2 响应详情

```bash
curl http://127.0.0.1:8091/health

{"status":"ok","time":"2026-04-15T08:34:02+00:00"}
```

## 5. 环境变量

### 5.1 当前配置

| 变量 | 值 | 说明 |
|------|-----|------|
| `CRM_DB_PATH` | 默认 `database/crm.sqlite` | SQLite 数据文件路径 |
| `TZ` | Asia/Shanghai | 运行时展示时区 |

### 5.2 敏感信息

| 变量 | 状态 |
|------|------|
| 默认管理员密码 | ✅ 已通过种子写入，可后续自行修改 |
| 数据库文件 | ✅ 本地单文件存储 |
| 销售演示账号 | ✅ 已写入 `sales / sales123456`、`sales2 / sales223456` |

## 6. 服务管理

### 6.1 常用命令

```bash
# 初始化数据库
php scripts/init-db.php

# 启动本地服务
php scripts/serve.php 127.0.0.1:8091

# 运行测试
php scripts/test.php

# 备份数据库
php scripts/backup-db.php
```

### 6.2 停止服务

```bash
# 在当前终端按 Ctrl+C
```

## 7. 故障排查

### 7.1 常见问题

#### 问题 1：端口被占用

```bash
lsof -i :8091
kill -9 <PID>
```

#### 问题 2：数据库需要重置

```bash
php scripts/reset-db.php
```

#### 问题 3：登录失败

```text
默认账号：admin
默认密码：admin123456
```
